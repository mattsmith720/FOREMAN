# dependencies
node_modules/

# build output
dist/
.next/
out/
*.tsbuildinfo

# env (examples are committed; real env files are not)
.env
.env.local
.env.*.local
!.env.example
!**/.env.example
!**/.env.local.example

# secrets and credentials
*.pem
*.key
credentials.json
service-account*.json

# logs
*.log
npm-debug.log*

# python bytecode (build-whisper-dataset.py) + training export output
__pycache__/
*.pyc
backend/exports/
exports/

# agent skills (local tooling, not needed for deploy)
.agents/
skills-lock.json

# editor / local tooling (may contain project refs)
.cursor/mcp.json

# native local overrides
native/ios/**/Local.xcconfig
native/ios/**/RealGlasses.xcconfig

# playwright artifacts (web, landing, CI)
test-results/
playwright-report/
blob-report/

# os / editor
.DS_Store
.vercel
.vscode/
.idea/
