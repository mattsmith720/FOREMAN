# Web client environment. Copy to web/.env.local

# --- Local same-Wi-Fi testing (npm run dev:phone) ---
NEXT_PUBLIC_API_URL=same-origin
BACKEND_URL=http://127.0.0.1:8080

# --- Vercel production (set in Vercel dashboard, not in .env.local) ---
# NEXT_PUBLIC_API_URL=same-origin
# BACKEND_URL=https://foreman-api.onrender.com
# FOREMAN_API_KEY=shared-secret-if-backend-requires-it

# --- Fallback: phone calls API directly (skip Vercel /api proxy) ---
# NEXT_PUBLIC_API_URL=https://foreman-api.onrender.com
