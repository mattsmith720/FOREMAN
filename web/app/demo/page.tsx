import { DemoCoach } from "../../components/demo-coach";

export const metadata = {
  title: "Foreman Demo",
  description:
    "One-tap scripted CER install demo using A1 fixtures — no API key required.",
};

export default function DemoPage() {
  return <DemoCoach />;
}
